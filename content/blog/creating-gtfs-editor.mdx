# My Documentation

Here is how the logic works:

<Presentation>
  <div>
    ### Introduction to GTFS
    GTFS (General Transit Feed Specification) defines a common format for public transportation schedules and associated geographic information.
    
    It allow public transit agencies to publish their transit data in a format that can be consumed by a wide variety of software applications.
  </div>

  <div>
    ### Profile Image
    Here is an example structure of our team.
    
    <Image src="/profile.jpg" alt="Team Profile" width={400} height={400} className="mx-auto" />
  </div>

  <div>
    ### Code Example
    Here is how we parse the GTFS data using Node.js streams.

    ```typescript
    import { parse } from 'csv-parse';
    import fs from 'fs';

    const processFile = async (filePath: string) => {
      const parser = fs
        .createReadStream(filePath)
        .pipe(parse({ columns: true }));

      for await (const record of parser) {
        // Process each record efficiently
        console.log(record);
        await saveToDatabase(record);
      }
    };
    ```
  </div>

  <div>
    ### Long Content & Preview
    Sometimes we need to display a lot of information in one slide.
    
    1. First point about complexity
    2. Second point about performance
    3. Third point about scalability
    4. Fourth point about maintainability
    
    > Optimization is key when handling large datasets like GTFS.
    
    And here is a small preview component:
    
    <div className="p-4 border rounded bg-background shadow-sm my-4">
      <h4 className="font-bold">Live Status</h4>
      <div className="flex gap-2 mt-2">
        <span className="w-3 h-3 rounded-full bg-green-500"></span>
        <span>System Operational</span>
      </div>
    </div>
  </div>
</Presentation>